<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.collab.mapper.DocumentMapper">

    <select id="selectUserDocuments" resultType="com.collab.entity.Document">
        SELECT d.* FROM document d WHERE d.owner_id = #{userId} AND d.status != 0
        UNION
        SELECT d.* FROM document d
        INNER JOIN document_member dm ON d.id = dm.document_id
        WHERE dm.user_id = #{userId} AND d.status != 0
        UNION
        SELECT d.* FROM document d WHERE d.is_public = 1 AND d.status != 0
        ORDER BY updated_at DESC
    </select>

    <select id="selectSharedDocuments" resultType="com.collab.entity.Document">
        SELECT d.* FROM document d
        INNER JOIN document_member dm ON d.id = dm.document_id
        WHERE dm.user_id = #{userId} AND d.owner_id != #{userId} AND d.status != 0
        UNION
        SELECT d.* FROM document d
        WHERE d.is_public = 1 AND d.owner_id != #{userId} AND d.status != 0
        ORDER BY updated_at DESC
    </select>

    <select id="selectOwnDocuments" resultType="com.collab.entity.Document">
        SELECT * FROM document WHERE owner_id = #{userId} AND status != 0
        ORDER BY updated_at DESC
    </select>

</mapper>
